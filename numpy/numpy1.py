import numpy as np
#First you have to import the library so you can access the built in functions
array = np.array([['A','B','C'],['D','E','F'],['G','H','I']])
#Here we use the np as it is the keyword as we have impoter the numpy and it can be envoked by np 
# Here we have created a 2d array 
print(array.ndim)
#ndim is used to print the dimension of the arrray 
#We use the numpy array as it is faster than the tupple or list in the python 