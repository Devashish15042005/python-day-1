import numpy as np
array = np.array([1, 2, 3, 4, 5])
print(array +1)
print(array * 2)
print(array - 1)
print(array / 2)
print(array ** 2)
# Here we can see some scaler arithmetic operations on the array.
print(np.sqrt(array))
print(np.round(array))
print(np.floor(array))
print(np.ceil(array))
# Here we see some vectorized mathematical functions on the array.
